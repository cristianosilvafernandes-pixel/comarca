# Comarca Honorários — variáveis de ambiente
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Secretos — só servidor / CI
SUPABASE_SERVICE_ROLE_KEY=
# Extração de documentos (procuração/contrato) — usada no route handler do Next
# (server-side / Vercel). NÃO precisa ser NEXT_PUBLIC.
ANTHROPIC_API_KEY=
